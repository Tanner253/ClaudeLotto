# ============================================
# CLAUDE LOTTO - ENVIRONMENT CONFIGURATION
# Copy this file to .env.local and fill in your values
# ============================================

# Claude API
ANTHROPIC_API_KEY=

# MongoDB
MONGODB_URI=

# Solana Configuration
SOLANA_PRIVATE_KEY=
SOLANA_RPC_URL=https://api.devnet.solana.com
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Wallet Addresses
TREASURY_WALLET=
NEXT_PUBLIC_TREASURY_WALLET=
DEV_WALLET=

# Game Configuration
MESSAGE_COST_SOL=0.1
NEXT_PUBLIC_MESSAGE_COST=0.1
WINNER_PERCENTAGE=0.8
DEV_PERCENTAGE=0.2

# ============================================
# SECURITY / ADMIN (NEW - REQUIRED)
# ============================================

# Admin secret for accessing admin endpoints (win review, payout approval)
# Generate a strong random string: openssl rand -hex 32
ADMIN_SECRET=

# ============================================
# SECURITY NOTES
# ============================================
#
# 1. NEVER commit .env.local to version control
# 2. Use different keys for development and production
# 3. For production, consider:
#    - Using a hardware wallet for the treasury
#    - Implementing multi-signature for payouts
#    - Using a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
# 4. Rotate the ADMIN_SECRET periodically
# 5. Use a dedicated RPC provider for production (rate limits, reliability)
#
